<h1>This is the blog index page for mvp.</h1>

<% @posts.each do |post| %>
  <div class="post_wrapper">
    <h2 class="title"><%= link_to post.title, post %></h2>
    <p class="date">Posted on <%= post.created_at.strftime("%B, %d, %Y") %></p>
  </div>
<% end %>