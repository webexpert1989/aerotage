Dear <%= @search.user.display_name %>,

New <%= @search.target_class.to_s.downcase.pluralize %> appeared that match your saved search named "<%= @search.title %>". To view them, please follow the link(s) below:

<% @listings.each do |listing| %>
    <%= listing.title %> ( <%= listing.job? ? job_url(listing) : resume_url(listing) %> )
<% end %>
