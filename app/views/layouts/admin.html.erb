<html>
  <head><%= render 'admin/head' %></head>

  <body class="<%= yield(:scripts) if content_for?(:scripts) %>">
    <%= render 'admin/header' %>
    <%= render 'admin/menu' %>

    <div class="main-content clear">
      <%= render 'layouts/flash' %>
      <%= yield %>
    </div>
    <%= debug(params) if Rails.env.development? %>
  </body>
</html>
